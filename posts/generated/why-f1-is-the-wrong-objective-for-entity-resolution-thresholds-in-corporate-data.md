---
title: "Why F1 Is the Wrong Objective for Entity Resolution Thresholds in Corporate Data"
slug: "why-f1-is-the-wrong-objective-for-entity-resolution-thresholds-in-corporate-data"
author: "Tae Kim"
source: "devto_python"
published: "Thu, 06 Aug 2026 02:35:36 +0000"
description: "Most entity resolution pipelines are tuned to maximize F1. For corporate data, that is the wrong objective. F1 treats false positives and false negatives as ..."
keywords: "self, merge, false, auto, entity, review, corporate, positive"
generated: "2026-08-06T03:12:22.481835"
---

# Why F1 Is the Wrong Objective for Entity Resolution Thresholds in Corporate Data

## Overview

Most entity resolution pipelines are tuned to maximize F1. For corporate data, that is the wrong objective. F1 treats false positives and false negatives as equally costly. In practice they are not even close. A false negative in corporate ER means you have two records that refer to the same company — a redundancy you can fix later by rerunning the pipeline with updated parameters. A false positive means you merged two distinct companies into one entity. Every query that touches that node now returns blended facts from two unrelated organizations. The damage is structural and hard to detect because the merged entity looks valid. It has real attributes, real edges, real documents. Downstream systems have no signal that anything is wrong. The Production Failure That Exposed This I ran into this building er-api , an entity resolution layer for trade and corporate registry data. The initial Splink model was tuned on an F1-maximizing grid search. It performed well on held-out test pairs. In production, it merged a South Korean steel manufacturer with a logistics subsidiary that shared part of its name and a city. Both entities were real, both had filings, and the combined record passed every sanity check that had been written for individual entities. The merged node looked completely valid to any downstream query: Real attributes from both companies Edges connecting to real counterparties Trade documents that appeared to corroborate each other The error was invisible at the F1 level. It only surfaced when a user noticed that a steel exporter appeared to also own a refrigerated warehouse network. The fix was not a better model. It was a different threshold structure. Three Changes That Changed the Behavior 1. Split the decision space into three bands Instead of a single match/no-match threshold, use three bands: from dataclasses import dataclass from enum import Enum class ERDecision ( Enum ): AUTO_MERGE = " auto_merge " REVIEW_QUEUE = " review_queue " AUTO_REJECT = " auto_reject " @dataclass class ThresholdBands : auto_merge_floor : float = 0.95 # above this: auto-merge review_floor : float = 0.70 # between review_floor and auto_merge_floor: review queue # below review_floor: auto-reject def classify_match ( probability : float , bands : ThresholdBands ) -> ERDecision : if probability >= bands . auto_merge_floor : return ERDecision . AUTO_MERGE elif probability >= bands . review_floor : return ERDecision . REVIEW_QUEUE else : return ERDecision . AUTO_REJECT The bands were calibrated on domain-specific false positive cost , not test set F1. The auto-merge zone is deliberately narrow because the penalty for being wrong there is high. Most entities that a naive F1-tuned model would auto-merge end up in the review queue under this structure. In practice: on a corpus of East Asian corporate entities, moving from a single threshold (0.85) to banded thresholds (0.95/0.70) reduced false positive rate by 73% with a 12% increase in review queue volume. The review queue increase is recoverable. The false positive reduction is not available at all under single-threshold tuning. 2. Require multiple blocking key agreement for auto-merge A high match probability on name similarity alone is insufficient for the auto-merge band. The model must also agree on at least one structural attribute: @dataclass class AutoMergeGuard : """ Name-only high-confidence matches route to review instead of auto-merge. At least one structural attribute must agree before auto-merge triggers. """ structural_keys = [ " registration_number_prefix " , # first 6 chars of company reg number " jurisdiction_code " , # ISO country + subdivision " address_locality " , # city-level normalized ] def check ( self , candidate_pair : dict ) -> bool : name_sim = candidate_pair . get ( " name_similarity " , 0 ) if name_sim < 0.95 : return True # not name-only structural_agreement = any ( candidate_pair . get ( f " { key } _match " , False ) for key in self . structural_keys ) return structural_agreement This guard catches the class of error where two subsidiaries of different parent companies share a similar trading name in the same industry. Name similarity alone can push the Splink score above 0.95. The structural key requirement stops the auto-merge. 3. Track false positive rate separately F1 hides false positive rate behind a harmonic mean. Pull it out as a first-class metric: from datetime import datetime class ThresholdMonitor : def __init__ ( self , fp_rate_alert_threshold : float = 0.001 ): self . decisions = [] self . confirmed_fp = 0 self . auto_merge_count = 0 self . fp_rate_alert_threshold = fp_rate_alert_threshold def record_decision ( self , decision : ERDecision , probability : float ): self . decisions . append ({ " decision " : decision , " probability " : probability , " timestamp " : datetime . utcnow () }) if decision == ERDecision . AUTO_MERGE : self . auto_merge_count += 1 def record_confirmed_false_positive ( self ): self . confirmed_fp += 1 @property def false_positive_rate ( self ) -> float : if self . auto_merge_count == 0 : return 0.0 return self . confirmed_fp / self . auto_merge_count def is_alert ( self ) -> bool : return self . false_positive_rate > self . fp_rate_alert_threshold The alert threshold matters here. A threshold that stays at 0.1% false positive rate on a corpus of millions of entity pairs produces recoverable errors — the review team can handle the correction volume. At 1%, the review queue cannot keep up and bad merges accumulate faster than they can be fixed. The graph degrades quietly. The Principle Optimizing for F1 is the right starting point for model selection. It is the wrong objective for threshold deployment. In domains where a wrong merge is harder to reverse than a missed merge — corporate registries, medical records, financial entity graphs — the calibration step needs its own cost model. The cost model inputs are: Estimated effort to detect a bad merge (can be zero if you have no downstream audit) Estimated effort to correct a bad merge (graph surgery, downstream cache invalidation, restatement) Estimated throughput of the review queue (determines how much to shift from auto-merge to review) F1 assumes a symmetric cost. Most production entity graphs don't have a symmetric cost. The threshold structure should reflect that. I'm building er-api as an entity resolution layer for East Asian corporate registry and trade document data. The asymmetric cost model described here is live in production. If you're running ER pipelines on corporate data and hitting similar false positive issues, I'm happy to compare notes.

## Key Insights

This article was discovered from the latest RSS feeds and automatically transformed into a readable blog post.

### What You Should Know

- Trending topic in the developer community
- Relevant technology discussion
- Worth exploring for deeper research

## Original Source

https://dev.to/hannune/why-f1-is-the-wrong-objective-for-entity-resolution-thresholds-in-corporate-data-4m9g

## Conclusion

Technology moves quickly. Following curated RSS feeds helps developers stay informed about emerging tools, frameworks, and industry trends.
